📐 Обзор архитектуры проекта VPN-бота
Проект использует архитектуру, основанную на принципах Предметно-ориентированного проектирования (DDD) и Чистой архитектуры, что позволяет гибко интегрировать несколько внешних систем VPN-панелей и различных пользовательских интерфейсов.

Основные компоненты и их роли
1. Ядро приложения (Domain и Application)
Эти слои представляют собой независимую бизнес-логику проекта. Они не знают о Telegram, Mini App, Django или конкретной VPN-панели.

Слой Домена (Domain): Содержит все бизнес-правила и сущности (User, Subscription, Payment). Здесь определяются Интерфейсы (IVpnGateway), которые служат контрактами для работы с внешними VPN-системами.

Слой Приложения (Application): Содержит сервисы (SubscriptionService, UserService), которые оркестрируют выполнение бизнес-операций. Например, SubscriptionService решает, когда нужно вызвать метод создать_пользователя из абстрактного шлюза IVpnGateway.

2. Инфраструктурный шлюз (Django REST API)
Центральный компонент, отвечающий за обработку всех входящих запросов от пользовательских интерфейсов.

Django REST API: Служит единой точкой входа (API Gateway). Он принимает HTTP-запросы от Next.js Mini App и может быть использован самим Telegram-ботом. Его задача — получить запрос, авторизовать пользователя, обратиться к сервисам из слоя Application и вернуть результат в формате JSON. Он также управляет базой данных, используя ORM для сохранения и извлечения сущностей (User, Subscription, Payment).

3. Интерфейс Mini App (Next.js + Tailwind)
Предоставляет пользователю полноценный, интерактивный интерфейс для управления услугами.

Next.js Mini App: Это внешний клиент, запущенный на отдельном сервере. Он полностью отвечает за отображение данных, навигацию и взаимодействие с пользователем (покупка, просмотр статистики). Он взаимодействует исключительно с Django REST API для получения и отправки данных, никогда не обращаясь напрямую к боту или VPN-панелям.

4. Адаптеры VPN-систем (VPN Gateways)
Это ключевой механизм для интеграции различных VPN-панелей.

VPN Gateway (Адаптеры): Находятся в слое Инфраструктуры. Для каждой внешней VPN-системы (например, Панель X или Панель Y) создается отдельный класс-адаптер (например, ПанельX_Gateway, ПанельY_Gateway), который реализует контракт IVpnGateway.

Функция: Когда SubscriptionService (из слоя Application) запрашивает создание пользователя, адаптер переводит этот унифицированный запрос в специфический формат API конкретной VPN-панели (3x-ui или Amnezia WG) и преобразует ответ обратно в формат, понятный Домену.

5. Механизм Уведомлений (aiogram Bot)
Telegram-бот переходит от роли основного интерфейса к роли специализированного работника.

aiogram Bot: Работает как асинхронный "рабочий", сосредоточенный на двух задачах:

Точка входа: Обрабатывает команду /start и предоставляет пользователю кнопку для открытия Next.js Mini App (deeplink).

Уведомления: Периодически запускается, проверяет базу данных (через Django) на предмет истекающих подписок и отправляет пользователям проактивные оповещения о необходимости продления.

Резюмируя взаимодействие:

Mini App запрашивает данные у Django REST API → Django вызывает сервисы из Application → Сервисы обращаются к абстрактному IVpnGateway → Конкретный VPN Gateway (Адаптер) (Панель X или Y) преобразует запрос и взаимодействует с внешней VPN-панелью. Telegram-бот работает в фоновом режиме, отправляя уведомления и служа точкой входа в Mini App.


эта часть telegram bot